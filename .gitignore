__pycache__/
*.pyc
.venv/
*.egg-info/

# dist/ 는 빌드 산출물이지만 PDF 문서는 배포 대상이라 git에 포함
# Python wheel/sdist 빌드 시 생성되는 파일만 무시
dist/*.whl
dist/*.tar.gz

# graphify knowledge graph (로컬 빌드 산출물)
graphify-out/
